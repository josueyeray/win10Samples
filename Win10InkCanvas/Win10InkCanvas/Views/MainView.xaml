<base:ViewBase
    x:Class="Win10InkCanvas.Views.MainView"
    xmlns:base="using:Win10InkCanvas.Views.Base"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Win10InkCanvas.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding MainVM, Source={StaticResource Locator}}"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="24,24,0,0"> 
            <TextBlock Text="Natural writting &amp; ink" FontSize="36"/>
            <Button Content="Basic ink" Width="200" Command="{Binding NavigateToBasicInkCommand}"/>
            <Button Content="Multiple inks" Width="200" Command="{Binding NavigateToMultipleSurfacesCommand}" Margin="0,12,0,0"/>
            <Button Content="Using MVVM" Width="200" Command="{Binding NavigateToMultipleSurfacesMVVMCommand}" Margin="0, 12, 0, 0"/>
        </StackPanel>
        <Image Source="/Assets/hand.png" Stretch="Uniform" VerticalAlignment="Bottom" HorizontalAlignment="Right" Height="450" Margin="0,0,-40,-40"/>
    </Grid>
</base:ViewBase>
